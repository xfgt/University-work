The files in the Spring 2025 - sem 4/039 directory are not worked form an IDE, but rather from the raw compile process (GCC) from the terminal.

The directory that's linked to an IDE is ./entry;
There it's used by CLion.

That is the main reason the directory is seperated.